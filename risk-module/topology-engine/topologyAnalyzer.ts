/**
 * Understands network segments and topology.
 */

import type { TopologyDefinition } from "../data-layer/dataLoader";

export function getTopologyAnalysis(_topology: TopologyDefinition): unknown {
  return {};
}
